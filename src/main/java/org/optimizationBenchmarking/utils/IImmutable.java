package org.optimizationBenchmarking.utils;

/**
 * This is a tagging interface to be implemented only by immutable objects.
 * The general contract of this interface is that all getters of an object
 * implementing this interface always return the same objects or values,
 * which in turn must be immutable again, although not necessarily
 * implement this interface.
 */
public interface IImmutable {
  // empty
}
