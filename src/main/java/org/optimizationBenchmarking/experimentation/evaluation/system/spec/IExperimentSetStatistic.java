package org.optimizationBenchmarking.experimentation.evaluation.system.spec;

/**
 * This interface is common to statistics over the whole experiment set.
 * Such modules are only executed if the experiment set contains more than
 * one experiment.
 */
public interface IExperimentSetStatistic extends IExperimentSetModule {
  //
}
