package org.optimizationBenchmarking.experimentation.evaluation.system.spec;

/**
 * This interface is the basis for modules which perform per-experiment
 * statistics. For each experiment in the experiment set, one job will be
 * created.
 */
public interface IExperimentStatistic extends IExperimentModule {
  //
}
